# Core
opencv-python-headless>=4.10
numpy>=1.26
torch>=2.3
torchvision>=0.18
Pillow>=10.2
pydantic>=2.6

# Detection — YOLO11 + YOLO-World (open-vocab)
ultralytics>=8.3

# Face detection — InsightFace SCRFD (96.1% mAP on WiderFace)
insightface>=0.7
onnxruntime>=1.17

# Face detection fallback — MediaPipe
mediapipe>=0.10

# Pose estimation — ViTPose++ via MMPose
mmpose>=1.3
mmdet>=3.3
mmengine>=0.10

# Edge VLM — Qwen2.5-VL-3B
transformers>=4.45
accelerate>=0.33

# Audio — BEATs / AST (98.1% mAP on AudioSet)
sounddevice>=0.4
# tensorflow-hub for YAMNet fallback only
tensorflow-hub>=0.16

# Event emission
google-cloud-pubsub>=2.21
google-cloud-storage>=2.16
