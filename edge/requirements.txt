# Core
opencv-python-headless>=4.11
numpy>=1.26
torch>=2.5
torchvision>=0.20
Pillow>=11.0
pydantic>=2.10

# Detection — YOLO11 + YOLO-World v2 (open-vocab)
ultralytics>=8.3

# Face detection — InsightFace SCRFD (96.1% mAP on WiderFace)
insightface>=0.7
onnxruntime>=1.20

# Face detection fallback — MediaPipe
mediapipe>=0.10

# Pose estimation — ViTPose++ via MMPose
mmpose>=1.3
mmdet>=3.3
mmengine>=0.10

# Edge VLM — Qwen3-VL-4B → Qwen2.5-VL-7B → Qwen2.5-VL-3B → InternVL3-2B
transformers>=4.47
accelerate>=1.2

# Audio — BEATs / AST (98.1% mAP on AudioSet)
sounddevice>=0.5
# tensorflow-hub for YAMNet fallback only
tensorflow-hub>=0.16

# Event emission
google-cloud-pubsub>=2.27
google-cloud-storage>=2.19
