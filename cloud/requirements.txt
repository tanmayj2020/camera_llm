# API framework
fastapi>=0.115
uvicorn>=0.30

# Knowledge graph
graphiti-core>=0.5
neo4j>=5.23

# Vector database
qdrant-client>=1.11

# GCP services
google-cloud-pubsub>=2.23
google-cloud-bigquery>=3.25
google-cloud-storage>=2.18
google-cloud-aiplatform>=1.60

# LLM / Agent framework
langchain>=0.3
langgraph>=0.2
langchain-google-genai>=2.0

# VLM — Gemini 2.5 Pro
google-generativeai>=0.8

# ML / embeddings
numpy>=1.26
scipy>=1.14
scikit-learn>=1.5
torch>=2.3
transformers>=4.45

# Vision-language embeddings — SigLIP (SOTA)
# Falls back to OpenCLIP EVA02
open-clip-torch>=2.26

# Sentence embeddings — GTE-Qwen2 (MTEB #1)
sentence-transformers>=3.0

# Depth estimation — Depth Anything V2
# (loaded via transformers)

# Re-ID — CLIP-based person re-identification
# (loaded via transformers)

# Persistence — PostgreSQL + pgvector
psycopg[binary]>=3.2
psycopg-pool>=3.2
pgvector>=0.3

# Auth
pydantic>=2.8
httpx>=0.27
PyJWT>=2.9

# Observability — OpenTelemetry
opentelemetry-api>=1.25
opentelemetry-sdk>=1.25
opentelemetry-exporter-otlp-proto-grpc>=1.25
opentelemetry-instrumentation-fastapi>=0.46b
