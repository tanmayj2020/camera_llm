# API framework
fastapi>=0.115
uvicorn>=0.32

# Knowledge graph
graphiti-core>=0.5
neo4j>=5.25

# Vector database
qdrant-client>=1.13

# GCP services
google-cloud-pubsub>=2.27
google-cloud-bigquery>=3.27
google-cloud-storage>=2.19
google-cloud-aiplatform>=1.72

# LLM / Agent framework
langchain>=0.3
langgraph>=0.2
langchain-google-genai>=2.0

# VLM — Gemini 2.5 Pro (swap to gemini-3.0-pro when available)
google-generativeai>=0.8

# ML / embeddings
numpy>=1.26
scipy>=1.14
scikit-learn>=1.6
torch>=2.5
transformers>=4.47

# Vision-language embeddings — SigLIP (SOTA)
# Falls back to OpenCLIP EVA02
open-clip-torch>=2.29

# Sentence embeddings — GTE-Qwen2 (MTEB #1), BGE-M3 fallback
sentence-transformers>=3.3

# Depth estimation — Depth Anything V2
# (loaded via transformers)

# Re-ID — CLIP ViT-B/16 → DINOv2 ViT-B/14 → ResNet50
# CLIP loaded via clip package; DINOv2 via torch.hub
clip @ git+https://github.com/openai/CLIP.git

# Persistence — PostgreSQL + pgvector
psycopg[binary]>=3.2
psycopg-pool>=3.2
pgvector>=0.3

# Auth
pydantic>=2.10
httpx>=0.28
PyJWT>=2.10

# Observability — OpenTelemetry
opentelemetry-api>=1.29
opentelemetry-sdk>=1.29
opentelemetry-exporter-otlp-proto-grpc>=1.29
opentelemetry-instrumentation-fastapi>=0.50b
